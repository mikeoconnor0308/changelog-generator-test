name: Changelog

on:
  push:
    branches: main

jobs:
  Changelog:
    runs-on: ubuntu-latest
    name: Generate changelog
    steps:
      - uses: actions/checkout@v2
      
      - name: get short SHA
        id: vars
        run: |
         echo ::set-output name=sha_short::$(git rev-parse --short=4 ${{ github.sha }})

      - name: Generate Changelog
        id: changelog
        uses: heinrichreimer/github-changelog-generator-action@v2.1.1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          headerLabel: "# Changelog"
          breakingLabel: "Breaking"
          enhancementLabel: "** Enhancements **"
          bugsLabel: Bug fixes
          deprecatedLabel: '### Deprecations'
          removedLabel: "### Removals"
          securityLabel: "### Security"
          issuesLabel: "** 📁 Other issues **"
          prLabel: "** 📁 Other issues **"
          issues: true
          issuesWoLabels: true
          pullRequests: true
          prWoLabels: true
          author: true
          unreleased: true
          stripGeneratorNotice: true
          verbose: true

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v3
        with:
          branch: changelog-autoupdate-${{ steps.vars.outputs.sha_short }}
          branch-suffix: timestamp
          title: Changelog Update
          commit-message: Update CHANGELOG.md
      # USE TO COMMIT DIRECTLY TO MASTER
      #- name: Commit and push changelog
      #  run: |
      #    git config --local user.email github-action@entos.ai
      #    git config --local user.name github-action
      #    cat CHANGELOG.md
      #    git add CHANGELOG.md && git commit -m 'Updated CHANGELOG.md' && git push || echo "No changes to CHANGELOG.md"
